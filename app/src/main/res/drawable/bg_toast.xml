<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置透明背景色 -->
    <solid android:color="@color/bg_toast_color" />
    <!-- 设置四个圆角的半径 -->
    <corners
        android:radius="4dp" />
    <!-- 设置一下边距,让空间大一点 -->
    <padding
        android:bottom="14dp"
        android:left="54dp"
        android:right="54dp"
        android:top="14dp" />
</shape>